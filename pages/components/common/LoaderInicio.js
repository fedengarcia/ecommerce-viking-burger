import React from 'react';

const LoaderInicio = () => {
    return (
        <div id="contenedor">
            <div className="loader" id="loader">Loading...</div>
        </div>
    );
}

export default LoaderInicio;
